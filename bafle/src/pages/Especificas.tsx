import { PageShell } from "../components/PageShell";
import { SpecificTools } from "../components/SpecificTools";

/* ============================================================
   PÁGINA §02: HERRAMIENTAS ESPECÍFICAS (kit interactivo)
   ============================================================ */

export function EspecificasPage() {
  return (
    <PageShell id="especificas">
      <SpecificTools />
    </PageShell>
  );
}
