import { PageShell } from "../components/PageShell";
import { Polarity } from "../components/Polarity";

/* ============================================================
   PÁGINA §04: POLARIDAD (diagrama interactivo)
   ============================================================ */

export function PolaridadPage() {
  return (
    <PageShell id="polaridad">
      <Polarity />
    </PageShell>
  );
}
